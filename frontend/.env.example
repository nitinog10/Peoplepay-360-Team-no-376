# Copy to .env.local. Every var the browser reads must be NEXT_PUBLIC_*: Next 16
# removed serverRuntimeConfig/publicRuntimeConfig, and this app is client-rendered.

# Root of the API, including the version prefix. No trailing slash.
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
